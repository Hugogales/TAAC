# Core dependencies
torch>=2.0.0
numpy>=1.21.0
gymnasium>=0.29.0
pettingzoo>=1.24.0
supersuit>=3.9.0

# Environment-specific dependencies
# Standard PettingZoo environments
# For additional environments, install as needed:
# pip install 'pettingzoo[all]'  # All environments
# pip install 'pettingzoo[atari]'  # Atari only
# pip install 'pettingzoo[mpe]'  # MPE only

# Additional utilities
matplotlib>=3.5.0
pyyaml>=6.0
tqdm>=4.64.0

# BoxJump dependencies
swig>=4.0.0
box2d-py>=2.3.5
pygame>=2.1.0
# Note: box2d-py can be problematic - use conda for easier installation



# Development dependencies (optional)
jupyter>=1.0.0
ipython>=8.0.0

# Note: For BoxJump environment, check specific installation requirements
# Note: For GPU support, install appropriate CUDA version of PyTorch 